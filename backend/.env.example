PORT=4000
NODE_ENV=development

# Shared secret — the Next.js frontend sends this header on every backend request
INTERNAL_API_SECRET=your-strong-secret-here

# The deployed frontend URL — used for CORS (set to your Vercel URL in production)
FRONTEND_URL=http://localhost:3000
